<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/mainlayoutId"
    android:background="@drawable/gradient_background_phos_calc"
    tools:context=".activities.TapOnPhone.LoadingAnimationActivity">

  <androidx.cardview.widget.CardView
      android:id="@+id/gifCard"
      android:layout_width="74dp"
      android:layout_height="74dp"
      android:layout_centerInParent="true"
      app:cardBackgroundColor="@color/white_bg"
      app:cardCornerRadius="8dp">

    <ImageView
        android:id="@+id/GifImageView"
        android:layout_width="42dp"
        android:layout_height="42dp"
        android:layout_gravity="center"
        android:layout_centerInParent="true" />

  </androidx.cardview.widget.CardView>

  <TextView
      android:id="@+id/tvAnimationText"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_below="@+id/gifCard"
      android:layout_centerHorizontal="true"
      android:layout_marginTop="8dp"
      android:fontFamily="@font/montserrat_medium"
      android:gravity="center_horizontal"
      android:text="Validación de seguridad en proceso, \n por favor espera..."
      android:textColor="@color/white_bg"
      android:textSize="12sp" />
</RelativeLayout>